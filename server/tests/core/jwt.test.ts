import { test, describe } from 'vitest'
// import { signToken } from './../../src/core/Jwt'

describe('JWT methods', () => {
  test('Should return a token string', () => {
    // const token = signToken('', process.env.TEST_EMAIL!, 'MonkiG')
  })
})
